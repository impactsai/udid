{
  "name": "udid-standard",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Decision and Impact Determination draft standard and conformance fixtures",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:facilitator": "npm --prefix apps/facilitator run check"
  },
  "engines": {"node": ">=20"}
}
